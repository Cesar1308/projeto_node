{
  "name": "projeto_node",
  "version": "1.0.0",
  "description": "projeto node",
  "type": "module",
  "main": "teste.js",
  "scripts": {
    "dev": "npx nodemon src/app.js",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:fix": "npx eslint ./src --ext .js --fix --ígnore-path ./.gitignore",
    "lint:format": "npx prettier --loglevel warn --write \"./src/**/*.{js,ts}\" ",
    "lint": "npm run lint:format && npm run lint:fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  },
  "pre-commir": "lint"
}
